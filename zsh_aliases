alias m="mvim ."

alias r=rails
alias be="bundle exec"
alias all="be rspec --format=doc --format=NcFirstFail" # run all specs and notify at the first failure
#
# alias speedtest='curl -o /dev/null http://speedtest.wdc01.softlayer.com/downloads/test10.zip'
#
# #alias generate_tags="ctags -R --languages=ruby --exclude=.git --exclude=log . $(bundle list --paths)"
#
alias so="source"
#
# # git
alias gs="git status"
alias gl="git log"
alias ga="git add"
alias gd="git diff"
alias gco="git commit"
alias gch="git checkout"
alias grom="git rebase origin/master"
alias gr="git rebase"
alias gb="git branch"


alias e='open -a /Applications/Emacs.app "$1"'

alias gpull="git pull --rebase origin master"
#
# # growl notify
# alias gn="growlnotify -m done"
#
# alias grep='grep --color=auto'
alias vim=nvim
alias vi=nvim
alias v="vimr . -n"

alias zshrc='$EDITOR ~/.zshrc'
alias t='tail -f'

alias dc='docker-compose run --entrypoint /bin/bash --rm $1'
